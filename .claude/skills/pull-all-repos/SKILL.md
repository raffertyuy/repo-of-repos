---
name: pull-all-repos
description: Clone missing repos from repos/repos.yaml, pull existing ones, and regenerate repos.md
user-invocable: true
---

# Sync Workspace

Read `repos/repos.yaml` at the project root and ensure all declared repositories are cloned and up to date.

## Order of Operations

1. **Parse** `repos/repos.yaml` — read the `repos:` list
2. **Clone missing repos** — for each entry where `repos/<name>/` doesn't exist, clone it
3. **Pull existing repos** — for each entry where `repos/<name>/` already exists, fetch and pull
4. **Detect orphans** — identify directories in `repos/` that aren't declared in `repos/repos.yaml` and warn (don't delete)
5. **Regenerate repos.md** — update `repos/repos.md` with current repo analysis

## Parsing repos/repos.yaml

Read `repos/repos.yaml` from the project root. Each entry has:

- `name` (required) — directory name under `repos/`
- `url` (required) — git clone URL
- `branch` (optional, default: `main`) — branch to track
- `prefix` (optional) — task routing prefix for `_tasks/` system
- `description` (optional) — one-line summary

If `repos/repos.yaml` has an empty list (`repos: []`), inform the user and suggest adding entries or using `/add-repository`.

## Cloning Missing Repos

For each entry where `repos/<name>/` does NOT exist:

1. Run `git clone <url> repos/<name>`
2. If a `branch` is specified and it's not the default, run `git -C repos/<name> checkout <branch>`
3. Report: cloned successfully or error

## Pulling Existing Repos

For each entry where `repos/<name>/` already exists:

1. Verify it's a git repo (has `.git` directory)
2. Run `git -C repos/<name> fetch --all --prune`
3. Check the current branch: `git -C repos/<name> branch --show-current`
4. If the branch has an upstream, run `git -C repos/<name> pull --ff-only`
   - If fast-forward fails, report and skip — **never force-pull**
   - If uncommitted changes block the pull, report and skip
5. Report: updated (with new commit count), already up to date, or error

## Detecting Orphans

List directories in `repos/` and compare against `repos/repos.yaml` entries. For any directory that exists on disk but isn't in the manifest, warn:

> ⚠ `repos/<name>` exists on disk but is not declared in `repos/repos.yaml`. Run `/add-repository` to register it, or remove the directory.

Do NOT delete orphan directories.

## Regenerate repos.md

After all sync operations, regenerate `repos/repos.md`. For each repo:

1. Read its README.md (or README) if one exists
2. Check `package.json`, `Cargo.toml`, `go.mod`, `pyproject.toml`, or equivalent for tech stack
3. Read git remote URL: `git -C repos/<name> remote get-url origin`
4. Briefly scan top-level directory structure
5. Check which agent instruction files exist: `CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`

Write `repos/repos.md` with this format:

```markdown
# Repositories

This file describes each repository cloned into `repos/`. Auto-generated by `/pull-all-repos`. Do not edit manually.

## <repo-name>

- **Remote**: <git remote URL>
- **Tech stack**: <languages, frameworks, key dependencies>
- **Description**: <1-2 sentence summary>
- **Key paths**: <notable top-level directories>
- **Agent instructions**: <which of CLAUDE.md, AGENTS.md, .github/copilot-instructions.md exist, or "None">
```

If `repos/` has no directories, keep the placeholder comment.

## Summary

Report a table at the end:

| Repo | Status |
|------|--------|
| `<name>` | Cloned / Updated (N new commits) / Already up to date / Error: `<reason>` / ⚠ Orphan |

## Notes

- Never force-pull or reset sub-repos
- Never discard uncommitted local changes
- Run clones and pulls sequentially to avoid credential prompt conflicts
- If `repos/repos.yaml` doesn't exist, create it.
